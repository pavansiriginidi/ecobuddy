# Frontend (Vite) variables. Copy to .env for local development.
# Only use VITE_* here so the values are exposed to the browser intentionally.
VITE_API_BASE_URL=/api

# Optional Pexels key for frontend image lookups.
# Put your Pexels API key here in the root .env file.
VITE_PEXELS_API_KEY=
